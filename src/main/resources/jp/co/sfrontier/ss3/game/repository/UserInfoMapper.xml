<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="jp.co.sfrontier.ss3.game.repository.UserInfoMapper">

	<!-- 全件検索 -->
	<select id="findAll"
		resultType="jp.co.sfrontier.ss3.game.model.UserInfoModel">
		SELECT
		user_id,
		user_name,
		mail_address,
		status,
		create_datetime,
		update_datetime,
		version
		FROM
		user_information_tbl
		ORDER BY
		user_id
	</select>
	
	<!-- 主キー検索 -->
	<select id="findById"
		resultType="jp.co.sfrontier.ss3.game.model.UserInfoModel">
		SELECT
		user_id,
		user_name,
		mail_address,
		status,
		create_datetime,
		update_datetime,
		version
		FROM
		user_information_tbl
		WHERE
		user_id = #{id}
	</select>
	
	<!-- 条件検索(IDと名前) -->
	<select id="search"
		resultType="jp.co.sfrontier.ss3.game.model.UserInfoModel">
		SELECT
		user_id,
		user_name,
		mail_address,
		status,
		create_datetime,
		update_datetime,
		version
		FROM
		user_information_tbl
		WHERE
		1 = 1
		<if test="id != null">
			AND user_id = #{id}
		</if>
		<if test="name != null and name != ''">
			AND user_name LIKE CONCAT('%', #{name}, '%')
		</if>
		ORDER BY
		user_id
	</select>
	
	<!-- status更新のみ -->
	<update id="updateStatus">
		UPDATE
		user_information_tbl
		SET
		status = #{status},
		update_datetime = CURRENT_TIMESTAMP,
		version = version + 1
		WHERE
		user_id
		= #{userId}
		AND version = #{version}
	</update>
	
</mapper>